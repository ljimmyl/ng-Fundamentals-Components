import { ExponentiationPipe } from './exponentiation.pipe';

describe('ExponentiationPipe', () => {
  it('create an instance', () => {
    const pipe = new ExponentiationPipe();
    expect(pipe).toBeTruthy();
  });
});
