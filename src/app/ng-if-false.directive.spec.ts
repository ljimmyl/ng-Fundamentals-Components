import { NgIfFalseDirective } from './ng-if-false.directive';

describe('NgIfFalseDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfFalseDirective();
    expect(directive).toBeTruthy();
  });
});
